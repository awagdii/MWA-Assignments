import { IsVisableDirective } from './is-visable.directive';

describe('IsVisableDirective', () => {
  it('should create an instance', () => {
    const directive = new IsVisableDirective();
    expect(directive).toBeTruthy();
  });
});
